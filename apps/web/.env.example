# Sample .env.local for local development
# Copy this to .env.local and fill in values

# Public API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Google OAuth client ID — same value as backend GOOGLE_CLIENT_ID
# Required to enable the Google Sign-In button on login/register pages
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
